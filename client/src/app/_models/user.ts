export interface User{
    username: string;
    token:string;
}

/*
let data: number | string =43;
data=10;
data="hello";

interface Car{
    color: string;
    model: string;
    topSpeed?: number
}

let car1:Car={
    color:"blue",
    model:'fiat'
}


let car2:Car={
    color:"blue",
    model:'fiat',
    topSpeed:100
}

const multiply=(x:number,y:number):number=>{return x*y;};
*/